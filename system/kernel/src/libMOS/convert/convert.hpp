
#ifndef CONVERT_HPP
#define CONVERT_HPP

namespace Convert
{
	void ToString(uint64_t iC, char sString[]);
	void ToString(uint64_t iC, char sString[], uint8_t iBase);
	//string ToString(uint64_t iC);
}

#endif