
#include "heap.hpp"

namespace Heap
{
	void *Alloc(uint64_t size)
	{
		void *addr;
		return addr;
	}

	void *Alloc(uint64_t size, uint32_t align)
	{
		void *addr;
		return addr;
	}

	void Free(void *addr)
	{

	}
}